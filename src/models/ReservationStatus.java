public enum ReservationStatus {
    PENDING,
    CONFIRMED,
    CANCELLED
}

// This enum represents the status of a reservation in a cinema booking system.
// It can be either PENDING, CONFIRMED, or CANCELLED.
