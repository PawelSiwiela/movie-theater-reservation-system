package models;

public enum SeatStatus {
    AVAILABLE,
    RESERVED,
    OCCUPIED
}
// This enum represents the status of a seat in a cinema booking system.
// It can be either AVAILABLE, RESERVED, or OCCUPIED.